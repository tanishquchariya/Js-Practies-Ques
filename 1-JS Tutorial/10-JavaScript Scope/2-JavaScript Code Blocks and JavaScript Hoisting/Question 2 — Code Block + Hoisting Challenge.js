// 🔴 Question 2 — Code Block + Hoisting Challenge

// Create a Student Result System using code blocks.

// Start with:

// var studentName = "Rahul";
// let marks = 80;
// const passingMarks = 40;

// Create a function called:

// calculateResult()

// Inside the function:

// Before declaring it, print a variable called result using console.log().
// Then declare:
// var result = "Pass";
// Create an if block that checks:
// marks >= passingMarks
// Inside the if block:
// Create a let variable called message with value "Student Passed".
// Create a const variable called grade with value "A".
// Create a var variable called status with value "Excellent".
// Print message, grade, and status inside the block.
// After the if block, print result and status.
// Try to print message and grade after the block and observe what happens.
// Finally, print studentName, marks, and passingMarks outside the function.
// 🎯 Your challenge

// You need to understand both topics at the same time:

// What gets hoisted?
// What gets initialized?
// Which variables belong to the function?
// Which variables belong only to the { } block?
// Why can status be accessed outside the if block?
// Why can't message and grade be accessed outside the if block?
// ⚠️ Important

// For Question 2, don't remove the statements that cause an error. First run the program and observe the error. Then comment out the problematic line(s) and run it again to see the remaining output.